<% if !@alexa_rank.empty? %>
  <table>
    <tr>
      <th>Rank</th>
      <th>Date</th>
    </tr>
    <% @alexa_rank.each do |site| %>
      <tr>
        <td><%= site.rank %></td>
        <td><%= site.created_at %></td>
      </tr>
    <% end %>
  </table>
  <%= link_to "Back", :back %>
<% end %>
